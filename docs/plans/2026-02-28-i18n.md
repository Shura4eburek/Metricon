# Multilingual Support (i18n) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** Add RU/EN/UA language switching with flag buttons in the navbar and localStorage persistence using client-side JS.

**Architecture:** One `static/js/i18n.js` file holds all translation dictionaries and exposes `window.I18N`. HTML elements get `data-i18n="key"` attributes; `applyLang(lang)` iterates them and sets text. JS inline strings (confirm, alert, uptime units) call `I18N.t('key')` directly.

**Tech Stack:** Vanilla JS, localStorage, Django templates, Bootstrap 5

---

### Task 1: Create static/js/i18n.js

**Files:**
- Create: `static/js/i18n.js`

**Step 1: Create the file with this exact content**

```javascript
/**
 * Metricon i18n — client-side translations for EN / RU / UA.
 * Usage:
 *   I18N.t('key')          — get translation for current language
 *   I18N.applyLang('ru')   — switch language and re-render all [data-i18n] elements
 *   I18N.currentLang()     — returns saved lang code ('en' | 'ru' | 'ua')
 */
(function () {
  const TRANSLATIONS = {
    en: {
      // Navigation
      'nav.overview':  'Overview',
      'nav.api_keys':  'API Keys',

      // Status badges
      'status.online':   'ONLINE',
      'status.offline':  'OFFLINE',
      'status.active':   'Active',
      'status.inactive': 'Inactive',
      'status.inactive_badge': 'INACTIVE',

      // Overview page
      'overview.title':        'Bot Overview',
      'overview.online_of':    'online',
      'overview.refreshed_at': 'Refreshed at',
      'overview.no_bots':      'No bots registered yet.',
      'overview.register_btn': 'Register a bot',
      'overview.grid_title':   'Grid view',
      'overview.list_title':   'List view',
      'overview.never_seen':   'Never seen',
      'overview.last_seen':    'Last seen',
      'overview.ago':          'ago',
      'overview.never':        'Never',
      'overview.update_badge': 'Update needed',

      // Overview list columns
      'col.name':      'Name',
      'col.version':   'Version',
      'col.cpu':       'CPU',
      'col.ram':       'RAM',
      'col.uptime':    'Uptime',
      'col.req_hr':    'req/hr',
      'col.avg_ms':    'avg ms',
      'col.errors':    'errors',
      'col.last_seen': 'Last seen',

      // Uptime units
      'uptime.d':      'd',
      'uptime.h':      'h',
      'uptime.m':      'm',
      'uptime.less1m': '< 1m',

      // Bot detail page
      'detail.uptime':       'Uptime',
      'detail.cpu':          'CPU',
      'detail.memory':       'Memory',
      'detail.connections':  'Connections',
      'detail.req_24h':      'Req (24h)',
      'detail.errors_24h':   'Errors (24h)',
      'detail.in_24h':       'in 24h',
      'detail.chart_req':    'Requests per bucket',
      'detail.chart_cmds':   'Top Commands (24h)',
      'detail.chart_resp':   'Avg Response Time (ms)',
      'detail.chart_res':    'CPU & Memory over time',
      'detail.recent_errors':'Recent Errors',
      'detail.no_errors':    'No errors recorded.',
      'detail.err_time':     'Time',
      'detail.err_type':     'Type',
      'detail.err_cmd':      'Command',
      'detail.err_msg':      'Message',
      'detail.tb_btn':       'TB',

      // API Keys page
      'apikeys.title':       'Bot API Keys',
      'apikeys.push_update': 'Push Update',
      'apikeys.register':    'Register New Bot',
      'apikeys.col_name':    'Name',
      'apikeys.col_status':  'Status',
      'apikeys.col_key':     'API Key',
      'apikeys.col_created': 'Created',
      'apikeys.col_seen':    'Last Seen',
      'apikeys.col_actions': 'Actions',
      'apikeys.no_bots':     'No bots yet. Register one to get started.',
      'apikeys.modal_title': 'Register New Bot',
      'apikeys.label_name':  'Bot Name',
      'apikeys.label_desc':  'Description',
      'apikeys.cancel':      'Cancel',
      'apikeys.submit':      'Register',
      'apikeys.ph_name':     'my-trading-bot',
      'apikeys.ph_desc':     'Optional description',
      'apikeys.title_reveal':    'Reveal key',
      'apikeys.title_regen':     'Regenerate API key',
      'apikeys.title_delete':    'Delete bot',
      'apikeys.title_deactivate':'Deactivate',
      'apikeys.title_activate':  'Activate',

      // JS strings (confirm / alert)
      'js.regen_confirm':  'Regenerate API key? The old key will stop working immediately.',
      'js.regen_new_key':  'New key: ',
      'js.delete_confirm': 'Delete bot "{name}"? This will permanently remove all its metrics and data.',
      'js.push_confirm':   'Push latest client version to all bots? They will auto-update on next heartbeat.',
      'js.push_success':   'Update pushed! Bots will receive version {version} on next heartbeat.',
      'js.failed':         'Failed: ',
      'js.delete_failed':  'Failed to delete bot.',
      'js.push_failed':    'Failed to push update.',
      'js.registered':     'Bot "{name}" registered!',
      'js.save_key':       'Save this API key — it won\'t be shown in full again:',
    },

    ru: {
      'nav.overview':  'Обзор',
      'nav.api_keys':  'API Ключи',

      'status.online':        'ОНЛАЙН',
      'status.offline':       'ОФЛАЙН',
      'status.active':        'Активен',
      'status.inactive':      'Неактивен',
      'status.inactive_badge':'НЕАКТИВЕН',

      'overview.title':        'Обзор Ботов',
      'overview.online_of':    'онлайн',
      'overview.refreshed_at': 'Обновлено в',
      'overview.no_bots':      'Нет зарегистрированных ботов.',
      'overview.register_btn': 'Зарегистрировать бота',
      'overview.grid_title':   'Плитки',
      'overview.list_title':   'Список',
      'overview.never_seen':   'Не появлялся',
      'overview.last_seen':    'Был',
      'overview.ago':          'назад',
      'overview.never':        'Никогда',
      'overview.update_badge': 'Нужно обновление',

      'col.name':      'Имя',
      'col.version':   'Версия',
      'col.cpu':       'CPU',
      'col.ram':       'RAM',
      'col.uptime':    'Аптайм',
      'col.req_hr':    'зап/ч',
      'col.avg_ms':    'ср. мс',
      'col.errors':    'ошибки',
      'col.last_seen': 'Был онлайн',

      'uptime.d':      'д',
      'uptime.h':      'ч',
      'uptime.m':      'м',
      'uptime.less1m': '< 1м',

      'detail.uptime':       'Аптайм',
      'detail.cpu':          'CPU',
      'detail.memory':       'Память',
      'detail.connections':  'Соединения',
      'detail.req_24h':      'Запросы (24ч)',
      'detail.errors_24h':   'Ошибки (24ч)',
      'detail.in_24h':       'за 24ч',
      'detail.chart_req':    'Запросы по периодам',
      'detail.chart_cmds':   'Топ команд (24ч)',
      'detail.chart_resp':   'Среднее время ответа (мс)',
      'detail.chart_res':    'CPU и память',
      'detail.recent_errors':'Последние ошибки',
      'detail.no_errors':    'Ошибок не зафиксировано.',
      'detail.err_time':     'Время',
      'detail.err_type':     'Тип',
      'detail.err_cmd':      'Команда',
      'detail.err_msg':      'Сообщение',
      'detail.tb_btn':       'ТБ',

      'apikeys.title':       'API Ключи Ботов',
      'apikeys.push_update': 'Обновить клиент',
      'apikeys.register':    'Зарегистрировать бота',
      'apikeys.col_name':    'Имя',
      'apikeys.col_status':  'Статус',
      'apikeys.col_key':     'API Ключ',
      'apikeys.col_created': 'Создан',
      'apikeys.col_seen':    'Последний визит',
      'apikeys.col_actions': 'Действия',
      'apikeys.no_bots':     'Нет ботов. Зарегистрируйте первого.',
      'apikeys.modal_title': 'Зарегистрировать бота',
      'apikeys.label_name':  'Имя бота',
      'apikeys.label_desc':  'Описание',
      'apikeys.cancel':      'Отмена',
      'apikeys.submit':      'Зарегистрировать',
      'apikeys.ph_name':     'my-trading-bot',
      'apikeys.ph_desc':     'Необязательное описание',
      'apikeys.title_reveal':    'Показать ключ',
      'apikeys.title_regen':     'Сгенерировать новый ключ',
      'apikeys.title_delete':    'Удалить бота',
      'apikeys.title_deactivate':'Деактивировать',
      'apikeys.title_activate':  'Активировать',

      'js.regen_confirm':  'Сгенерировать новый API ключ? Старый ключ перестанет работать немедленно.',
      'js.regen_new_key':  'Новый ключ: ',
      'js.delete_confirm': 'Удалить бота "{name}"? Все метрики и данные будут удалены безвозвратно.',
      'js.push_confirm':   'Отправить последнюю версию клиента всем ботам? Они обновятся автоматически.',
      'js.push_success':   'Обновление отправлено! Боты получат версию {version} при следующем heartbeat.',
      'js.failed':         'Ошибка: ',
      'js.delete_failed':  'Не удалось удалить бота.',
      'js.push_failed':    'Не удалось отправить обновление.',
      'js.registered':     'Бот "{name}" зарегистрирован!',
      'js.save_key':       'Сохраните API ключ — он больше не будет показан полностью:',
    },

    ua: {
      'nav.overview':  'Огляд',
      'nav.api_keys':  'API Ключі',

      'status.online':        'ОНЛАЙН',
      'status.offline':       'ОФЛАЙН',
      'status.active':        'Активний',
      'status.inactive':      'Неактивний',
      'status.inactive_badge':'НЕАКТИВНИЙ',

      'overview.title':        'Огляд Ботів',
      'overview.online_of':    'онлайн',
      'overview.refreshed_at': 'Оновлено о',
      'overview.no_bots':      'Немає зареєстрованих ботів.',
      'overview.register_btn': 'Зареєструвати бота',
      'overview.grid_title':   'Плитки',
      'overview.list_title':   'Список',
      'overview.never_seen':   'Не з\'являвся',
      'overview.last_seen':    'Був',
      'overview.ago':          'тому',
      'overview.never':        'Ніколи',
      'overview.update_badge': 'Потрібне оновлення',

      'col.name':      'Ім\'я',
      'col.version':   'Версія',
      'col.cpu':       'CPU',
      'col.ram':       'RAM',
      'col.uptime':    'Аптайм',
      'col.req_hr':    'зап/год',
      'col.avg_ms':    'сер. мс',
      'col.errors':    'помилки',
      'col.last_seen': 'Був онлайн',

      'uptime.d':      'д',
      'uptime.h':      'г',
      'uptime.m':      'хв',
      'uptime.less1m': '< 1хв',

      'detail.uptime':       'Аптайм',
      'detail.cpu':          'CPU',
      'detail.memory':       'Пам\'ять',
      'detail.connections':  'З\'єднання',
      'detail.req_24h':      'Запити (24г)',
      'detail.errors_24h':   'Помилки (24г)',
      'detail.in_24h':       'за 24г',
      'detail.chart_req':    'Запити по періодах',
      'detail.chart_cmds':   'Топ команд (24г)',
      'detail.chart_resp':   'Середній час відповіді (мс)',
      'detail.chart_res':    'CPU та пам\'ять',
      'detail.recent_errors':'Останні помилки',
      'detail.no_errors':    'Помилок не зафіксовано.',
      'detail.err_time':     'Час',
      'detail.err_type':     'Тип',
      'detail.err_cmd':      'Команда',
      'detail.err_msg':      'Повідомлення',
      'detail.tb_btn':       'ТБ',

      'apikeys.title':       'API Ключі Ботів',
      'apikeys.push_update': 'Оновити клієнт',
      'apikeys.register':    'Зареєструвати бота',
      'apikeys.col_name':    'Ім\'я',
      'apikeys.col_status':  'Статус',
      'apikeys.col_key':     'API Ключ',
      'apikeys.col_created': 'Створено',
      'apikeys.col_seen':    'Останній візит',
      'apikeys.col_actions': 'Дії',
      'apikeys.no_bots':     'Немає ботів. Зареєструйте першого.',
      'apikeys.modal_title': 'Зареєструвати бота',
      'apikeys.label_name':  'Ім\'я бота',
      'apikeys.label_desc':  'Опис',
      'apikeys.cancel':      'Скасувати',
      'apikeys.submit':      'Зареєструвати',
      'apikeys.ph_name':     'my-trading-bot',
      'apikeys.ph_desc':     'Необов\'язковий опис',
      'apikeys.title_reveal':    'Показати ключ',
      'apikeys.title_regen':     'Згенерувати новий ключ',
      'apikeys.title_delete':    'Видалити бота',
      'apikeys.title_deactivate':'Деактивувати',
      'apikeys.title_activate':  'Активувати',

      'js.regen_confirm':  'Згенерувати новий API ключ? Старий ключ перестане працювати негайно.',
      'js.regen_new_key':  'Новий ключ: ',
      'js.delete_confirm': 'Видалити бота "{name}"? Всі метрики та дані будуть видалені безповоротно.',
      'js.push_confirm':   'Надіслати останню версію клієнта всім ботам? Вони оновляться автоматично.',
      'js.push_success':   'Оновлення надіслано! Боти отримають версію {version} при наступному heartbeat.',
      'js.failed':         'Помилка: ',
      'js.delete_failed':  'Не вдалося видалити бота.',
      'js.push_failed':    'Не вдалося надіслати оновлення.',
      'js.registered':     'Бот "{name}" зареєстрований!',
      'js.save_key':       'Збережіть API ключ — він більше не буде показаний повністю:',
    },
  };

  const LANG_KEY = 'metricon_lang';

  function currentLang() {
    return localStorage.getItem(LANG_KEY) || 'en';
  }

  function t(key) {
    const lang = currentLang();
    return (TRANSLATIONS[lang] && TRANSLATIONS[lang][key]) ||
           (TRANSLATIONS['en'] && TRANSLATIONS['en'][key]) ||
           key;
  }

  function applyLang(lang) {
    if (!TRANSLATIONS[lang]) return;
    localStorage.setItem(LANG_KEY, lang);

    // Text content
    document.querySelectorAll('[data-i18n]').forEach(el => {
      const key = el.dataset.i18n;
      const val = TRANSLATIONS[lang][key] || TRANSLATIONS['en'][key];
      if (val !== undefined) el.textContent = val;
    });

    // title attributes
    document.querySelectorAll('[data-i18n-title]').forEach(el => {
      const key = el.dataset.i18nTitle;
      const val = TRANSLATIONS[lang][key] || TRANSLATIONS['en'][key];
      if (val !== undefined) el.title = val;
    });

    // placeholder attributes
    document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
      const key = el.dataset.i18nPlaceholder;
      const val = TRANSLATIONS[lang][key] || TRANSLATIONS['en'][key];
      if (val !== undefined) el.placeholder = val;
    });

    // Flag button active state
    document.querySelectorAll('.lang-btn').forEach(btn => {
      btn.classList.toggle('active', btn.dataset.lang === lang);
    });
  }

  window.I18N = { t, applyLang, currentLang };
})();
```

**Step 2: Verify file was created**

```bash
ls C:/Users/Mamoru/PycharmProjects/Metricon/static/js/i18n.js
```

Expected: file exists

**Step 3: Commit**

```bash
cd C:/Users/Mamoru/PycharmProjects/Metricon && git add static/js/i18n.js && git commit -m "feat: add i18n translation dictionary (EN/RU/UA)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 2: Update base.html — load i18n.js + flag buttons

**Files:**
- Modify: `templates/base.html`

**Step 1: Add i18n.js in `<head>` before `{% block extra_head %}`**

Add this line immediately before `{% block extra_head %}{% endblock %}`:

```html
  <script src="/static/js/i18n.js"></script>
```

**Step 2: Replace the entire `<nav>` element**

Current nav (lines 38–52):
```html
<nav class="navbar navbar-expand-lg navbar-dark" style="background-color:#161b27; border-bottom:1px solid #2a2d3a;">
  <div class="container-fluid">
    <a class="navbar-brand text-info" href="/dashboard/">
      <i class="bi bi-graph-up-arrow me-2"></i>Metricon
    </a>
    <div class="navbar-nav ms-auto">
      <a class="nav-link {% block nav_overview %}{% endblock %}" href="/dashboard/">
        <i class="bi bi-grid-1x2 me-1"></i>Overview
      </a>
      <a class="nav-link {% block nav_apikeys %}{% endblock %}" href="/dashboard/api-keys/">
        <i class="bi bi-key me-1"></i>API Keys
      </a>
    </div>
  </div>
</nav>
```

Replace with:
```html
<nav class="navbar navbar-expand-lg navbar-dark" style="background-color:#161b27; border-bottom:1px solid #2a2d3a;">
  <div class="container-fluid">
    <a class="navbar-brand text-info" href="/dashboard/">
      <i class="bi bi-graph-up-arrow me-2"></i>Metricon
    </a>
    <div class="navbar-nav ms-auto d-flex align-items-center gap-2">
      <a class="nav-link {% block nav_overview %}{% endblock %}" href="/dashboard/">
        <i class="bi bi-grid-1x2 me-1"></i><span data-i18n="nav.overview">Overview</span>
      </a>
      <a class="nav-link {% block nav_apikeys %}{% endblock %}" href="/dashboard/api-keys/">
        <i class="bi bi-key me-1"></i><span data-i18n="nav.api_keys">API Keys</span>
      </a>
      <div class="d-flex gap-1 ms-3">
        <button class="lang-btn btn btn-sm btn-outline-secondary px-2 py-0" data-lang="en" onclick="I18N.applyLang('en')" title="English">🇬🇧</button>
        <button class="lang-btn btn btn-sm btn-outline-secondary px-2 py-0" data-lang="ru" onclick="I18N.applyLang('ru')" title="Русский">🇷🇺</button>
        <button class="lang-btn btn btn-sm btn-outline-secondary px-2 py-0" data-lang="ua" onclick="I18N.applyLang('ua')" title="Українська">🇺🇦</button>
      </div>
    </div>
  </div>
</nav>
```

**Step 3: Add `applyLang` call at end of `<body>`, right before `{% block extra_js %}`**

Add this line immediately before `{% block extra_js %}{% endblock %}`:

```html
<script>I18N.applyLang(I18N.currentLang());</script>
```

**Step 4: Commit**

```bash
cd C:/Users/Mamoru/PycharmProjects/Metricon && git add templates/base.html && git commit -m "feat: add language switcher flag buttons to navbar

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 3: Update overview.html — data-i18n attributes + JS strings

**Files:**
- Modify: `templates/dashboard/overview.html`

**Step 1: Read the file to confirm current content, then apply these changes**

Make ALL of the following replacements in `templates/dashboard/overview.html`:

**A) Page title h4:**
```html
<!-- BEFORE -->
    <i class="bi bi-grid-1x2 me-2 text-info"></i>Bot Overview
<!-- AFTER -->
    <i class="bi bi-grid-1x2 me-2 text-info"></i><span data-i18n="overview.title">Bot Overview</span>
```

**B) "online" counter text:**
```html
<!-- BEFORE -->
      &nbsp;·&nbsp;
      <span id="last-refresh" class="text-muted"></span>
<!-- AFTER -->
      &nbsp;·&nbsp;
      <span id="last-refresh" class="text-muted"></span>
      <span class="visually-hidden" data-i18n="overview.online_of">online</span>
```

Wait — the "online" word appears inline as plain text in: `{{ online_count }} / {{ total_count }} online`. It's not in a tag. Wrap it:

```html
<!-- BEFORE -->
      <span class="text-success fw-bold">{{ online_count }}</span> /
      <span class="text-light">{{ total_count }}</span> online
<!-- AFTER -->
      <span class="text-success fw-bold">{{ online_count }}</span> /
      <span class="text-light">{{ total_count }}</span> <span data-i18n="overview.online_of">online</span>
```

**C) Toggle button titles:**
```html
<!-- BEFORE -->
      <button id="btn-grid" type="button" class="btn btn-outline-secondary" onclick="setView('grid')" title="Grid view">
<!-- AFTER -->
      <button id="btn-grid" type="button" class="btn btn-outline-secondary" onclick="setView('grid')" data-i18n-title="overview.grid_title" title="Grid view">
```
```html
<!-- BEFORE -->
      <button id="btn-list" type="button" class="btn btn-outline-secondary" onclick="setView('list')" title="List view">
<!-- AFTER -->
      <button id="btn-list" type="button" class="btn btn-outline-secondary" onclick="setView('list')" data-i18n-title="overview.list_title" title="List view">
```

**D) Empty state:**
```html
<!-- BEFORE -->
      <p class="mt-3 text-muted">No bots registered yet.</p>
      <a href="/dashboard/api-keys/" class="btn btn-outline-info btn-sm">Register a bot</a>
<!-- AFTER -->
      <p class="mt-3 text-muted" data-i18n="overview.no_bots">No bots registered yet.</p>
      <a href="/dashboard/api-keys/" class="btn btn-outline-info btn-sm" data-i18n="overview.register_btn">Register a bot</a>
```

**E) Card footer "Last seen ... ago" and "Never seen":**
```html
<!-- BEFORE -->
          <span class="text-muted">
            {% if s.bot.last_seen_at %}
              Last seen {{ s.bot.last_seen_at|timesince }} ago
            {% else %}
              Never seen
            {% endif %}
          </span>
<!-- AFTER -->
          <span class="text-muted">
            {% if s.bot.last_seen_at %}
              <span data-i18n="overview.last_seen">Last seen</span> {{ s.bot.last_seen_at|timesince }} <span data-i18n="overview.ago">ago</span>
            {% else %}
              <span data-i18n="overview.never_seen">Never seen</span>
            {% endif %}
          </span>
```

**F) "Update needed" badge in cards:**
```html
<!-- BEFORE -->
                <span class="badge bg-warning text-dark" style="font-size:.65rem">Update needed</span>
<!-- AFTER -->
                <span class="badge bg-warning text-dark" style="font-size:.65rem" data-i18n="overview.update_badge">Update needed</span>
```

**G) "Update needed" badge in list (⚠ Update):**
```html
<!-- BEFORE -->
                <span class="badge bg-warning text-dark d-block mt-1" style="font-size:.6rem; width:fit-content">⚠ Update</span>
<!-- AFTER -->
                <span class="badge bg-warning text-dark d-block mt-1" style="font-size:.6rem; width:fit-content" data-i18n="overview.update_badge">Update needed</span>
```

**H) List table column headers:**
```html
<!-- BEFORE -->
          <th>Name</th>
          <th>Version</th>
          <th style="width:9rem">CPU</th>
          <th class="text-end" style="width:5rem">RAM</th>
          <th class="text-end" style="width:5rem">Uptime</th>
          <th class="text-end" style="width:4.5rem">req/hr</th>
          <th class="text-end" style="width:4.5rem">avg ms</th>
          <th class="text-end" style="width:4rem">errors</th>
          <th style="width:6rem">Last seen</th>
<!-- AFTER -->
          <th data-i18n="col.name">Name</th>
          <th data-i18n="col.version">Version</th>
          <th style="width:9rem" data-i18n="col.cpu">CPU</th>
          <th class="text-end" style="width:5rem" data-i18n="col.ram">RAM</th>
          <th class="text-end" style="width:5rem" data-i18n="col.uptime">Uptime</th>
          <th class="text-end" style="width:4.5rem" data-i18n="col.req_hr">req/hr</th>
          <th class="text-end" style="width:4.5rem" data-i18n="col.avg_ms">avg ms</th>
          <th class="text-end" style="width:4rem" data-i18n="col.errors">errors</th>
          <th style="width:6rem" data-i18n="col.last_seen">Last seen</th>
```

**I) List "Never" text:**
```html
<!-- BEFORE -->
            {% if s.bot.last_seen_at %}{{ s.bot.last_seen_at|timesince }} ago{% else %}Never{% endif %}
<!-- AFTER -->
            {% if s.bot.last_seen_at %}{{ s.bot.last_seen_at|timesince }} <span data-i18n="overview.ago">ago</span>{% else %}<span data-i18n="overview.never">Never</span>{% endif %}
```

**J) "Detail" button text in cards:**
```html
<!-- BEFORE (cards) -->
            Detail <i class="bi bi-arrow-right"></i>
<!-- AFTER (both occurrences — cards AND list) -->
            <span data-i18n="col.name" style="display:none"></span><i class="bi bi-arrow-right"></i>
```

Wait — "Detail" button text doesn't have a translation key yet. Add `'col.detail': 'Detail'` — no, actually let's keep it simple, "Detail" is widely understood. Skip translating the Detail button.

**K) Update `fmtUptime` function in JS to use I18N:**

In the `{% block extra_js %}` script, replace the `fmtUptime` function:

```javascript
// BEFORE
  function fmtUptime(s) {
    if (s == null) return '—';
    const d = Math.floor(s / 86400);
    const h = Math.floor((s % 86400) / 3600);
    const m = Math.floor((s % 3600) / 60);
    if (d > 0) return d + 'д ' + h + 'ч';
    if (h > 0) return h + 'ч ' + m + 'м';
    if (m > 0) return m + 'м';
    return '< 1м';
  }

// AFTER
  function fmtUptime(s) {
    if (s == null) return '—';
    const d = Math.floor(s / 86400);
    const h = Math.floor((s % 86400) / 3600);
    const m = Math.floor((s % 3600) / 60);
    if (d > 0) return d + I18N.t('uptime.d') + ' ' + h + I18N.t('uptime.h');
    if (h > 0) return h + I18N.t('uptime.h') + ' ' + m + I18N.t('uptime.m');
    if (m > 0) return m + I18N.t('uptime.m');
    return I18N.t('uptime.less1m');
  }
```

Also update `updateRefreshTime` to use translation:
```javascript
// BEFORE
    document.getElementById('last-refresh').textContent =
      'Refreshed at ' + new Date().toLocaleTimeString();

// AFTER
    document.getElementById('last-refresh').textContent =
      I18N.t('overview.refreshed_at') + ' ' + new Date().toLocaleTimeString();
```

**Step 2: Commit**

```bash
cd C:/Users/Mamoru/PycharmProjects/Metricon && git add templates/dashboard/overview.html && git commit -m "feat: add data-i18n attributes to overview template

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 4: Update bot_detail.html — data-i18n attributes + uptime JS

**Files:**
- Modify: `templates/dashboard/bot_detail.html`

**Step 1: Read the file, then apply these replacements**

**A) Stat tile labels (lines ~34,39,44,51,58,65):**
```html
<!-- BEFORE -->
      <div class="label">Uptime</div>
<!-- AFTER -->
      <div class="label" data-i18n="detail.uptime">Uptime</div>
```
```html
<!-- BEFORE -->
      <div class="label">CPU</div>
<!-- AFTER -->
      <div class="label" data-i18n="detail.cpu">CPU</div>
```
```html
<!-- BEFORE -->
      <div class="value text-primary">{{ last_hb.memory_mb|floatformat:0 }}<small class="fs-6">MB</small></div>
      <div class="label">Memory</div>
<!-- AFTER -->
      <div class="value text-primary">{{ last_hb.memory_mb|floatformat:0 }}<small class="fs-6">MB</small></div>
      <div class="label" data-i18n="detail.memory">Memory</div>
```
```html
<!-- BEFORE -->
      <div class="label">Connections</div>
<!-- AFTER -->
      <div class="label" data-i18n="detail.connections">Connections</div>
```
```html
<!-- BEFORE -->
      <div class="label">Req (24h)</div>
<!-- AFTER -->
      <div class="label" data-i18n="detail.req_24h">Req (24h)</div>
```
```html
<!-- BEFORE -->
      <div class="label">Errors (24h)</div>
<!-- AFTER -->
      <div class="label" data-i18n="detail.errors_24h">Errors (24h)</div>
```

**B) Chart headers:**
```html
<!-- BEFORE -->
      <div class="card-header text-muted small">Requests per bucket</div>
<!-- AFTER -->
      <div class="card-header text-muted small" data-i18n="detail.chart_req">Requests per bucket</div>
```
```html
<!-- BEFORE -->
      <div class="card-header text-muted small">Top Commands (24h)</div>
<!-- AFTER -->
      <div class="card-header text-muted small" data-i18n="detail.chart_cmds">Top Commands (24h)</div>
```
```html
<!-- BEFORE -->
      <div class="card-header text-muted small">Avg Response Time (ms)</div>
<!-- AFTER -->
      <div class="card-header text-muted small" data-i18n="detail.chart_resp">Avg Response Time (ms)</div>
```
```html
<!-- BEFORE -->
      <div class="card-header text-muted small">CPU & Memory over time</div>
<!-- AFTER -->
      <div class="card-header text-muted small" data-i18n="detail.chart_res">CPU & Memory over time</div>
```

**C) Error table header and badge:**
```html
<!-- BEFORE -->
    <span class="text-muted small">Recent Errors</span>
    <span class="badge bg-danger">{{ errors_24h }} in 24h</span>
<!-- AFTER -->
    <span class="text-muted small" data-i18n="detail.recent_errors">Recent Errors</span>
    <span class="badge bg-danger">{{ errors_24h }} <span data-i18n="detail.in_24h">in 24h</span></span>
```

**D) Error table column headers:**
```html
<!-- BEFORE -->
            <th>Time</th><th>Type</th><th>Command</th><th>Message</th><th></th>
<!-- AFTER -->
            <th data-i18n="detail.err_time">Time</th><th data-i18n="detail.err_type">Type</th><th data-i18n="detail.err_cmd">Command</th><th data-i18n="detail.err_msg">Message</th><th></th>
```

**E) "No errors recorded." and "TB" button:**
```html
<!-- BEFORE -->
    <p class="text-muted text-center py-3 mb-0">No errors recorded.</p>
<!-- AFTER -->
    <p class="text-muted text-center py-3 mb-0" data-i18n="detail.no_errors">No errors recorded.</p>
```
```html
<!-- BEFORE -->
              <button class="btn btn-outline-secondary btn-sm py-0"
                      data-bs-toggle="collapse"
                      data-bs-target="#tb-{{ err.id }}">
                TB
              </button>
<!-- AFTER -->
              <button class="btn btn-outline-secondary btn-sm py-0"
                      data-bs-toggle="collapse"
                      data-bs-target="#tb-{{ err.id }}"
                      data-i18n="detail.tb_btn">TB
              </button>
```

**F) Update uptime JS in extra_js block to use I18N:**
```javascript
// BEFORE
(function () {
  const el = document.getElementById('uptime-value');
  if (!el) return;
  const s = parseInt(el.dataset.seconds, 10);
  if (isNaN(s)) return;
  const d = Math.floor(s / 86400);
  const h = Math.floor((s % 86400) / 3600);
  const m = Math.floor((s % 3600) / 60);
  let fmt;
  if (d > 0)      fmt = d + 'д ' + h + 'ч';
  else if (h > 0) fmt = h + 'ч ' + m + 'м';
  else if (m > 0) fmt = m + 'м';
  else            fmt = '< 1м';
  el.textContent = fmt;
})();

// AFTER
(function () {
  const el = document.getElementById('uptime-value');
  if (!el) return;
  const s = parseInt(el.dataset.seconds, 10);
  if (isNaN(s)) return;
  const d = Math.floor(s / 86400);
  const h = Math.floor((s % 86400) / 3600);
  const m = Math.floor((s % 3600) / 60);
  let fmt;
  if (d > 0)      fmt = d + I18N.t('uptime.d') + ' ' + h + I18N.t('uptime.h');
  else if (h > 0) fmt = h + I18N.t('uptime.h') + ' ' + m + I18N.t('uptime.m');
  else if (m > 0) fmt = m + I18N.t('uptime.m');
  else            fmt = I18N.t('uptime.less1m');
  el.textContent = fmt;
})();
```

**Step 2: Commit**

```bash
cd C:/Users/Mamoru/PycharmProjects/Metricon && git add templates/dashboard/bot_detail.html && git commit -m "feat: add data-i18n attributes to bot detail template

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 5: Update api_keys.html — data-i18n + JS confirm/alert strings

**Files:**
- Modify: `templates/bots/api_keys.html`

**Step 1: Read the file, then apply these replacements**

**A) Page heading:**
```html
<!-- BEFORE -->
  <h4 class="mb-0 text-light"><i class="bi bi-key me-2 text-info"></i>Bot API Keys</h4>
<!-- AFTER -->
  <h4 class="mb-0 text-light"><i class="bi bi-key me-2 text-info"></i><span data-i18n="apikeys.title">Bot API Keys</span></h4>
```

**B) Header buttons:**
```html
<!-- BEFORE -->
    <button class="btn btn-outline-success btn-sm" onclick="pushUpdate()" title="Push latest client to all bots">
      <i class="bi bi-cloud-upload me-1"></i>Push Update
    </button>
    <button class="btn btn-info btn-sm" data-bs-toggle="modal" data-bs-target="#registerModal">
      <i class="bi bi-plus-lg me-1"></i>Register New Bot
    </button>
<!-- AFTER -->
    <button class="btn btn-outline-success btn-sm" onclick="pushUpdate()" data-i18n-title="apikeys.push_update" title="Push latest client to all bots">
      <i class="bi bi-cloud-upload me-1"></i><span data-i18n="apikeys.push_update">Push Update</span>
    </button>
    <button class="btn btn-info btn-sm" data-bs-toggle="modal" data-bs-target="#registerModal">
      <i class="bi bi-plus-lg me-1"></i><span data-i18n="apikeys.register">Register New Bot</span>
    </button>
```

**C) Table headers:**
```html
<!-- BEFORE -->
          <th>Name</th>
          <th>Status</th>
          <th>API Key</th>
          <th>Created</th>
          <th>Last Seen</th>
          <th>Actions</th>
<!-- AFTER -->
          <th data-i18n="apikeys.col_name">Name</th>
          <th data-i18n="apikeys.col_status">Status</th>
          <th data-i18n="apikeys.col_key">API Key</th>
          <th data-i18n="apikeys.col_created">Created</th>
          <th data-i18n="apikeys.col_seen">Last Seen</th>
          <th data-i18n="apikeys.col_actions">Actions</th>
```

**D) Status badges:**
```html
<!-- BEFORE -->
              <span class="badge bg-success">Active</span>
<!-- AFTER -->
              <span class="badge bg-success" data-i18n="status.active">Active</span>
```
```html
<!-- BEFORE -->
              <span class="badge bg-secondary">Inactive</span>
<!-- AFTER -->
              <span class="badge bg-secondary" data-i18n="status.inactive">Inactive</span>
```

**E) Action button titles:**
```html
<!-- BEFORE -->
                    title="Reveal key">
<!-- AFTER -->
                    data-i18n-title="apikeys.title_reveal" title="Reveal key">
```
```html
<!-- BEFORE -->
                    title="Regenerate API key">
<!-- AFTER -->
                    data-i18n-title="apikeys.title_regen" title="Regenerate API key">
```
```html
<!-- BEFORE -->
                    title="Delete bot">
<!-- AFTER -->
                    data-i18n-title="apikeys.title_delete" title="Delete bot">
```
```html
<!-- BEFORE -->
                    title="{% if bot.is_active %}Deactivate{% else %}Activate{% endif %}">
<!-- AFTER -->
                    data-i18n-title="{% if bot.is_active %}apikeys.title_deactivate{% else %}apikeys.title_activate{% endif %}" title="{% if bot.is_active %}Deactivate{% else %}Activate{% endif %}">
```

**F) Empty state:**
```html
<!-- BEFORE -->
    <p class="mt-3 text-muted">No bots yet. Register one to get started.</p>
<!-- AFTER -->
    <p class="mt-3 text-muted" data-i18n="apikeys.no_bots">No bots yet. Register one to get started.</p>
```

**G) Modal:**
```html
<!-- BEFORE -->
          <h5 class="modal-title text-light">Register New Bot</h5>
<!-- AFTER -->
          <h5 class="modal-title text-light" data-i18n="apikeys.modal_title">Register New Bot</h5>
```
```html
<!-- BEFORE -->
            <label class="form-label text-muted">Bot Name <span class="text-danger">*</span></label>
            <input type="text" name="name" class="form-control bg-dark text-light border-secondary"
                   placeholder="my-trading-bot" required>
<!-- AFTER -->
            <label class="form-label text-muted"><span data-i18n="apikeys.label_name">Bot Name</span> <span class="text-danger">*</span></label>
            <input type="text" name="name" class="form-control bg-dark text-light border-secondary"
                   data-i18n-placeholder="apikeys.ph_name" placeholder="my-trading-bot" required>
```
```html
<!-- BEFORE -->
            <label class="form-label text-muted">Description</label>
            <input type="text" name="description" class="form-control bg-dark text-light border-secondary"
                   placeholder="Optional description">
<!-- AFTER -->
            <label class="form-label text-muted" data-i18n="apikeys.label_desc">Description</label>
            <input type="text" name="description" class="form-control bg-dark text-light border-secondary"
                   data-i18n-placeholder="apikeys.ph_desc" placeholder="Optional description">
```
```html
<!-- BEFORE -->
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
          <button type="submit" class="btn btn-info">Register</button>
<!-- AFTER -->
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-i18n="apikeys.cancel">Cancel</button>
          <button type="submit" class="btn btn-info" data-i18n="apikeys.submit">Register</button>
```

**H) Replace ALL JS confirm/alert strings in `{% block extra_js %}` script:**

Replace the entire `<script>` block content (keep `<script>` and `</script>` tags, replace only the JS inside):

```javascript
const keyVisible = {};

function toggleKey(botId, fullKey) {
  const span = document.getElementById('key-' + botId);
  const btn = document.getElementById('reveal-btn-' + botId);
  if (keyVisible[botId]) {
    span.textContent = fullKey.slice(0, 8) + '•'.repeat(56);
    btn.innerHTML = '<i class="bi bi-eye"></i>';
    keyVisible[botId] = false;
  } else {
    span.textContent = fullKey;
    btn.innerHTML = '<i class="bi bi-eye-slash"></i>';
    keyVisible[botId] = true;
  }
}

async function regenerateKey(botId) {
  if (!confirm(I18N.t('js.regen_confirm'))) return;
  try {
    const res = await fetch(`/dashboard/api-keys/${botId}/regenerate/`, {method: 'POST'});
    if (res.ok) {
      const data = await res.json();
      document.getElementById('key-' + botId).textContent = data.api_key.slice(0, 8) + '•'.repeat(56);
      keyVisible[botId] = false;
      alert(I18N.t('js.regen_new_key') + data.api_key);
    }
  } catch (e) { alert(I18N.t('js.failed') + e.message); }
}

async function toggleActive(botId, btn) {
  try {
    const res = await fetch(`/dashboard/api-keys/${botId}/toggle/`, {method: 'POST'});
    if (res.ok) location.reload();
  } catch (e) { alert(I18N.t('js.failed') + e.message); }
}

async function deleteBot(botId, botName) {
  if (!confirm(I18N.t('js.delete_confirm').replace('{name}', botName))) return;
  try {
    const res = await fetch(`/dashboard/api-keys/${botId}/delete/`, {method: 'POST'});
    if (res.ok) {
      document.getElementById('row-' + botId).remove();
    } else {
      alert(I18N.t('js.delete_failed'));
    }
  } catch (e) { alert(I18N.t('js.failed') + e.message); }
}

async function pushUpdate() {
  if (!confirm(I18N.t('js.push_confirm'))) return;
  try {
    const res = await fetch('/dashboard/api-keys/push-update/', {method: 'POST'});
    if (res.ok) {
      const data = await res.json();
      alert(I18N.t('js.push_success').replace('{version}', data.version));
    } else {
      alert(I18N.t('js.push_failed'));
    }
  } catch (e) { alert(I18N.t('js.failed') + e.message); }
}
```

**Step 2: Commit**

```bash
cd C:/Users/Mamoru/PycharmProjects/Metricon && git add templates/bots/api_keys.html && git commit -m "feat: add data-i18n attributes and translated JS strings to api_keys template

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

### Task 6: Deploy and verify

**Step 1: Push to Railway**

```bash
cd C:/Users/Mamoru/PycharmProjects/Metricon && git push origin master
```

**Step 2: Verify on production**

Open https://web-production-37313.up.railway.app/dashboard/ and check:
- Flag buttons 🇬🇧 🇷🇺 🇺🇦 appear in the navbar
- Clicking 🇷🇺 switches all text to Russian immediately (no reload)
- Clicking 🇺🇦 switches to Ukrainian
- After page reload, selected language is restored
- Uptime shows `8ч 30м` in Russian, `8г 30хв` in Ukrainian, `8h 30m` in English
- Confirm dialogs in API Keys page appear in selected language
